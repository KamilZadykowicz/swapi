export type PersonModel = {
  name: string;
  mass: string;
};

export type StarshipModel = {
  name: string;
  crew: string;
};

export type CardModel = {
  name: string;
  points: number;
  gameCount: number;
};
